import React from 'react';

module.exports = {
    title: {
        type: 'textarea',
        required: true,
        label: 'feed.article.title',
    },
    content: {
        type: 'textarea',
        required: true,
        label: 'feed.article.content',
    },
};
