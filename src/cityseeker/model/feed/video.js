import React from 'react';

module.exports = {
    title: {
        type: 'textarea',
        required: false,
        label: 'feed.video.title',
    },
    content: {
        type: 'textarea',
        required: true,
        label: 'feed.video.content',
    },
};
