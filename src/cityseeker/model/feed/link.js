import React from 'react';

module.exports = {
    title: {
        type: 'textarea',
        required: true,
        label: 'feed.link.title',
    },
    content: {
        type: 'textarea',
        required: true,
        label: 'feed.link.content',
    },
};
